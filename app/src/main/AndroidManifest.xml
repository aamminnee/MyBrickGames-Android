<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- permission pour acceder a internet -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- permission pour afficher des notifications (obligatoire sur android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- permission pour afficher le telechargement en arriere-plan -->
    <uses-permission android:name="android.permission.DOWNLOAD_WITHOUT_NOTIFICATION" />

    <!-- permission d'ecriture uniquement requise pour android 9 et inferieur -->
    <!-- (les versions plus recentes n'en ont pas besoin pour le dossier telechargements) -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"
        tools:ignore="ScopedStorage" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.MyBrickGamesAndroid">

        <!-- ajout de configchanges pour eviter le rechargement de la webview lors de la rotation -->
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- declaration de notre nouvelle page native pour les parametres -->
        <activity
            android:name=".SettingsActivity"
            android:exported="false" />

    </application>

</manifest>